import { DefaultNode, DefaultStep, NodeID, NodeType, TraceFrame as BaseTraceFrame, TraceType } from './types';
export declare type StepData = {
    src: string;
    loop: boolean;
    customPause?: boolean;
};
export declare type NodeData = {
    src: string;
    loop: boolean;
    nextID?: NodeID;
    pauseID?: NodeID;
    previousID?: NodeID;
};
export declare enum TraceStreamAction {
    LOOP = "LOOP",
    PLAY = "PLAY",
    PAUSE = "PAUSE"
}
export declare type Step = DefaultStep<NodeType.STREAM, StepData>;
export declare type Node = DefaultNode<NodeType.STREAM, NodeData>;
export declare type TraceFrame = BaseTraceFrame<TraceType.STREAM, {
    src: string;
    action: TraceStreamAction;
    token: string;
}>;
