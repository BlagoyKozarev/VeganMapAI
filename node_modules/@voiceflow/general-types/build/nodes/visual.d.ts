import { DeviceType, Dimensions } from "../constants";
import { DefaultNode, DefaultStep, NodeID, NodeType, TraceFrame as DefaultTraceFrame, TraceType } from './types';
export declare enum CanvasVisibility {
    FULL = "full",
    CROPPED = "cropped"
}
export declare enum APLType {
    JSON = "JSON",
    SPLASH = "SPLASH"
}
export declare enum VisualType {
    APL = "apl",
    IMAGE = "image"
}
export declare type ImageStepData = {
    visualType: VisualType.IMAGE;
    image: string | null;
    device: DeviceType | null;
    dimensions: Dimensions | null;
    canvasVisibility: CanvasVisibility;
};
export declare type APLStepData = {
    visualType: VisualType.APL;
    title?: string;
    aplType: APLType;
    imageURL?: string;
    document?: string;
    datasource?: string;
    aplCommands?: string;
    jsonFileName?: string;
};
export declare type StepData = ImageStepData | APLStepData;
export declare type NodeData = {
    data: StepData;
    nextId?: NodeID;
};
export declare type Step = DefaultStep<NodeType.VISUAL, StepData>;
export declare type Node = DefaultNode<NodeType.VISUAL, NodeData>;
export declare type TraceFrame = DefaultTraceFrame<TraceType.VISUAL, StepData>;
