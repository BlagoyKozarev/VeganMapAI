import { DefaultNode, DefaultStep, NodeID, NodeType } from './types';
export declare type Set = {
    variable: string | null;
    expression: string;
};
export declare type StepData = {
    sets: Set[];
    title?: string;
};
export declare type NodeSet = {
    variable: string | null;
    expression: string | number;
};
export declare type NodeData = {
    sets: NodeSet[];
    nextId?: NodeID;
};
export declare type Step = DefaultStep<NodeType.SET_V2, StepData>;
export declare type Node = DefaultNode<NodeType.SET_V2, NodeData>;
