import { DefaultNode, DefaultStep, NodeID, NodeType } from './types';
export declare type StepData = {
    code: string;
};
export declare type NodeData = {
    code: string;
    fail_id?: NodeID;
    success_id?: NodeID;
};
export declare type Step = DefaultStep<NodeType.CODE, StepData>;
export declare type Node = DefaultNode<NodeType.CODE, NodeData>;
