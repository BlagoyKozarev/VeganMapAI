import { DefaultNode, DefaultStep, NodeID, NodeType, TraceFrame as DefaultTraceFrame, TraceType } from './types';
export declare type VariableMapping = [string | null, string | null][];
export declare type StepData = {
    diagramID: string | null;
    variableMap: null | {
        inputs: VariableMapping;
        outputs: VariableMapping;
    };
};
export declare type NodeData = {
    nextId?: NodeID;
    diagram_id?: string;
    variable_map?: {
        inputs?: [string, string][];
        outputs?: [string, string][];
    };
};
export declare type Step = DefaultStep<NodeType.FLOW, StepData>;
export declare type Node = DefaultNode<NodeType.FLOW, NodeData>;
export declare type TraceFrame = DefaultTraceFrame<TraceType.FLOW, {
    diagramID: string;
}>;
