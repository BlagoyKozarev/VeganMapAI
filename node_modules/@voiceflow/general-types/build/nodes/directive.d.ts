import { DefaultNode, DefaultStep, NodeType } from './types';
export declare type StepData = {
    directive: string;
};
export declare type NodeData = {
    directive: string;
    nextId: string;
};
export declare type Step = DefaultStep<NodeType.DIRECTIVE, StepData>;
export declare type Node = DefaultNode<NodeType.DIRECTIVE, NodeData>;
