import { DefaultNode, DefaultStep, Event, ExpressionData, NodeID, NodeType } from './types';
export declare type StepData = {
    expressions: ExpressionData[];
};
export declare type NodeData = {
    payload: {
        expressions: (string | number)[];
        elseId?: NodeID;
    };
    paths: Array<{
        event?: Event;
        nextID: NodeID | null;
    }>;
};
export declare type Step = DefaultStep<NodeType.IF_V2, StepData>;
export declare type Node = DefaultNode<NodeType.IF_V2, NodeData>;
