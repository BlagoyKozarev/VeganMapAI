import { Port, SlotMapping } from '@voiceflow/api-sdk';
import { DefaultStep, NodeType } from './types';
export declare type StepData = {
    name: string;
    intent: string | null;
    mappings: SlotMapping[];
    diagramID: string | null;
    ports: Port[];
};
export declare type Step = DefaultStep<NodeType.COMMAND, StepData>;
