import { Chip, Prompt } from "../types";
import { DefaultNode, DefaultStep, NodeID, NodeType } from './types';
export declare type StepData<V> = {
    slot: string | null;
    variable: string | null;
    reprompt: Prompt<V> | null;
    slotInputs: string[];
    chips: Chip[] | null;
};
export declare type NodeData = {
    nextId?: NodeID;
    variable: string;
    reprompt?: string;
    chips?: Chip[];
};
export declare type Step<V> = DefaultStep<NodeType.CAPTURE, StepData<V>>;
export declare type Node = DefaultNode<NodeType.CAPTURE, NodeData>;
