import { DefaultNode, DefaultStep, NodeType } from './types';
export declare enum RandomType {
    DEFAULT = 1,
    DO_DUPLICATES = 2
}
export declare type StepData = {
    paths: number;
    noDuplicates: boolean;
};
export declare type NodeData = {
    random: RandomType;
    nextIds: (string | null)[];
};
export declare type Step = DefaultStep<NodeType.RANDOM, StepData>;
export declare type Node = DefaultNode<NodeType.RANDOM, NodeData>;
