import { Port } from '@voiceflow/api-sdk';
import { DefaultNode, DefaultStep, Event, NodeID } from './types';
export declare const _V1_STOP_TYPES = "stopTypes";
export declare type StepData<B = unknown> = {
    _v: 1;
    payload: B;
    stop?: boolean;
    defaultPath?: number;
};
export declare type NodeData<E = Event> = {
    _v: 1;
    payload: unknown;
    stop: boolean;
    defaultPath?: number;
    paths: Array<{
        event?: E;
        nextID: NodeID | null;
    }>;
};
export declare type Node = DefaultNode<string, NodeData>;
export declare type Step<B = unknown, E = Event> = DefaultStep<string, StepData<B>, Port<{
    event?: E;
}>[]>;
