import { DefaultNode, DefaultStep, Expression, NodeID, NodeType } from './types';
export declare type StepData = {
    expressions: Expression[];
};
export declare type NodeData = {
    elseId?: NodeID;
    nextIds: (string | null)[];
    expressions: (string | number)[];
};
export declare type Step = DefaultStep<NodeType.IF, StepData>;
export declare type Node = DefaultNode<NodeType.IF, NodeData>;
