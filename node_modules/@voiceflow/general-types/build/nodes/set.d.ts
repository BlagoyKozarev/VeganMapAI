import { DefaultNode, DefaultStep, Expression, NodeID, NodeType } from './types';
export declare type Set = {
    variable: string | null;
    expression: Expression;
};
export declare type StepData = {
    sets: Set[];
};
export declare type NodeSet = {
    variable: string | null;
    expression: string | number;
};
export declare type NodeData = {
    sets: NodeSet[];
    nextId?: NodeID;
};
export declare type Step = DefaultStep<NodeType.SET, StepData>;
export declare type Node = DefaultNode<NodeType.SET, NodeData>;
