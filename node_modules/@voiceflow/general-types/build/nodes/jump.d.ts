import { SlotMapping } from '@voiceflow/api-sdk';
import { DefaultStep, NodeType } from './types';
export declare type StepData = {
    intent: string | null;
    mappings?: SlotMapping[];
};
export declare type Step = DefaultStep<NodeType.INTENT, StepData>;
