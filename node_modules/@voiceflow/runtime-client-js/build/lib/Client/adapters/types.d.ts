import { GeneralTrace as DBGeneralTrace } from '@voiceflow/general-types';
import { ResponseContext } from "../../types";
export declare type DBResponseContext = Omit<ResponseContext, 'trace'> & {
    trace: DBGeneralTrace[];
};
