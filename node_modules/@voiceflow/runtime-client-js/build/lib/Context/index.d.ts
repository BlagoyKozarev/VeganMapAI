import { Choice, ResponseContext } from "../types";
import VariableManager from '../Variables';
export declare class Context<S extends Record<string, any> = Record<string, any>> {
    private context;
    variables: VariableManager<S>;
    constructor(context: ResponseContext);
    getChips(): Choice[];
    getTrace(): import("../types").GeneralTrace[];
    toJSON(): ResponseContext;
    isEnding(): boolean;
    private setVariables;
}
export default Context;
