import { ResponseContext } from '../types';
declare class VariableManager<S extends Record<string, any>> {
    private _internalGetState;
    private _internalSetVars;
    constructor(_internalGetState: () => ResponseContext | null, _internalSetVars: (newVars: Partial<S>) => void);
    get<K extends keyof S>(key: K): S[K];
    getAll(): S;
    getKeys(): Array<keyof S>;
    set<K extends keyof S>(key: K, val: S[K]): void;
    setMany(newVars: Partial<S>): void;
    private getVariables;
}
export default VariableManager;
