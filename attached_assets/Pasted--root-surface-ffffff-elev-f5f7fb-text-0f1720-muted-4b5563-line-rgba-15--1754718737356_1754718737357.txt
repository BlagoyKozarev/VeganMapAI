:root{
  --surface:#ffffff;
  --elev:#f5f7fb;
  --text:#0f1720;
  --muted:#4b5563;
  --line:rgba(15,23,32,0.08);
  --accent:#22c55e;
  --accent-600:#16a34a;
  --danger:#ef4444;
}

.vm-bar{position:relative; z-index:5; margin:10px auto; max-width:1100px;}
.vm-card{background:var(--surface); border:1px solid var(--line); border-radius:14px; box-shadow:0 2px 10px rgba(0,0,0,.04); padding:10px;}
.vm-row{display:flex; gap:8px; flex-wrap:wrap; align-items:center}
.vm-chip{display:inline-flex; align-items:center; gap:6px; border:1px solid var(--line); border-radius:999px; padding:8px 12px; background:transparent; color:var(--text); font-weight:600; cursor:pointer}
.vm-chip:hover{background:var(--elev)}
.vm-chip input, .vm-chip select{border:0; background:transparent; color:inherit; outline:none}
.vm-spacer{flex:1}
.vm-btn{display:inline-flex; align-items:center; gap:8px; border:1px solid var(--line); border-radius:999px; padding:10px 14px; background:transparent; color:var(--text); font-weight:700; cursor:pointer}
.vm-btn:hover{background:var(--elev)}
.vm-btn-primary{background:var(--accent); color:#06240f; border:0}
.vm-btn-primary:hover{background:var(--accent-600)}

#vm-pop{position:absolute; z-index:10; display:none; min-width:260px; max-width:320px; background:var(--surface); border:1px solid var(--line); border-radius:12px; box-shadow:0 8px 30px rgba(0,0,0,.12); padding:12px}
#vm-pop h4{margin:0 0 6px; font-size:16px}
#vm-pop .muted{color:var(--muted); font-size:13px; margin-bottom:8px}
#vm-pop .actions{display:flex; flex-wrap:wrap; gap:8px}
#vm-pop .actions .vm-btn{padding:8px 12px}

#vm-score{position:fixed; top:0; right:0; height:100vh; width:360px; max-width:90vw; background:var(--surface); border-left:1px solid var(--line); box-shadow:-10px 0 30px rgba(0,0,0,.08); transform:translateX(100%); transition:transform .25s ease; z-index:20; display:flex; flex-direction:column}
#vm-score.open{transform:translateX(0)}
#vm-score header{display:flex; align-items:center; justify-content:space-between; padding:12px 14px; border-bottom:1px solid var(--line)}
#vm-score .panel{padding:12px 14px; overflow:auto}
.vm-kv{display:grid; grid-template-columns:1fr auto; gap:8px; margin:4px 0}
.vm-meter{height:8px; background:var(--elev); border-radius:6px; overflow:hidden}
.vm-meter > span{display:block; height:100%; background:var(--accent)}